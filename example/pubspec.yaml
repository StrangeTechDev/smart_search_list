name: smart_search_list_example
description: Example app demonstrating smart_search_list package
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

dependencies:
  material_ui: ^1.0.0
  flutter:
    sdk: flutter
  smart_search_list:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true